def get_num_words(file, total):
    count = len(file.split())
    total += count
    return total
def total_characters(file, total):
    #dictionary = {}
    #for char in file:
    #    if dictionary[char]
#    dictionary[char] = 

